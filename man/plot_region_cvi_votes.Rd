% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exploratory_graphics.R
\name{plot_region_cvi_votes}
\alias{plot_region_cvi_votes}
\title{Validity-index Agreement Along One Axis}
\usage{
plot_region_cvi_votes(regions, axis = NULL, score_col = "score")
}
\arguments{
\item{regions}{A \code{"regions_tuning"} object (or its \code{tuning_log}).}

\item{axis}{Axis to plot along. \code{NULL} (default) picks the axis with the most
evaluated candidates.}

\item{score_col}{Column used to locate the optimum. Default \code{"score"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Borda aggregation hides whether the constituent indices agree. This plots
each index along a single axis, rescaled to \verb{[0, 1]} and sign-corrected so
that higher is better in every case, with the other axes pinned at the
optimum. Indices that disagree about the location of the optimum are the
signal worth acting on: the Borda winner is only meaningful when the voters
are not pulling in opposite directions.
}
