% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modular_skate_w_tune.R
\name{evaluate_nrst_stability}
\alias{evaluate_nrst_stability}
\alias{print.nrst_stability}
\alias{plot.nrst_stability}
\title{Evaluate Coassociation Matrix Stability Across Ensemble Sizes}
\usage{
evaluate_nrst_stability(
  graph,
  trees,
  skater_regions,
  n.rst_candidates,
  stability_metric = c("mean_abs_diff", "frobenius", "correlation"),
  subsample_n = 5000,
  prior_strength = 0,
  seed = NULL,
  verbose = TRUE
)

\method{print}{nrst_stability}(x, ...)

\method{plot}{nrst_stability}(x, ...)
}
\arguments{
\item{graph}{The parent \code{igraph} object the trees were sampled from.
Required because membership re-computation needs the original edge set.}

\item{trees}{A \code{"skater_trees"} object from
\code{\link{sample_spanning_trees}}.}

\item{skater_regions}{Integer scalar \eqn{\ge 2}. Number of SKATER regions
used as anchor (typically the median of the candidate range).}

\item{n.rst_candidates}{Integer vector (each \eqn{\ge 2}). Ensemble sizes to
evaluate; all must be \code{<= trees$n_trees}.}

\item{stability_metric}{Character. One of \code{"mean_abs_diff"} (default),
\code{"frobenius"}, or \code{"correlation"}.}

\item{subsample_n}{Integer. Number of nodes to subsample for stability
comparisons, to avoid materialising N x N distance matrices on large
graphs. Default 5000.}

\item{prior_strength}{Non-negative numeric scalar \eqn{\lambda_E} passed to
the membership computation (see \code{\link{compute_ensemble_memberships}}).
Default 0 (no prior weighting).}

\item{seed}{Integer or \code{NULL}. Seed for the subsample selection; the
caller's RNG state is restored on exit.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{x}{An object of class \code{"nrst_stability"}.}

\item{...}{Further arguments. For \code{plot}, passed to \code{plot()};
for \code{print}, unused.}
}
\value{
An object of class \code{"nrst_stability"} — a list with:
\describe{
\item{\code{best_nrst}}{Selected ensemble size (knee point).}
\item{\code{stability}}{Data frame with columns \code{n.rst},
\code{change}, and \code{is_knee}.}
\item{\code{skater_regions}}{The anchor value used.}
\item{\code{metric}}{The stability metric used.}
\item{\code{prior_strength}}{The prior strength used.}
}

\code{print.nrst_stability} returns \code{x} invisibly.

\code{plot.nrst_stability} is called for its side effect (draws the
stability curve) and returns \code{NULL} invisibly.
}
\description{
Computes how much the consensus (Hamming) distance structure changes as more
spanning trees are added, and identifies a knee point where the marginal gain
plateaus. Used to choose the ensemble size \code{n.rst}.
}
\examples{
\dontrun{
stab <- evaluate_nrst_stability(g, trees, skater_regions = 20,
                                n.rst_candidates = c(5, 10, 15, 20))
plot(stab)
}

}
