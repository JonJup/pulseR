% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{plot_local_similarity}
\alias{plot_local_similarity}
\title{Plot cross-region similarity of local types}
\usage{
plot_local_similarity(
  local,
  distance = c("mean", "hellinger", "wasserstein"),
  k = NULL,
  h = NULL,
  groups = NULL,
  triangle = TRUE,
  ...
)
}
\arguments{
\item{local}{A fitted local-type object (uses \code{local$models}).}

\item{distance}{Component distance: \code{"mean"} (standardised component means,
matches the fingerprint plot), \code{"hellinger"} (mean and shape), or
\code{"wasserstein"}.}

\item{k, h}{Optional meta-clustering cut used \emph{only} to colour the axis labels:
\code{k} meta-types or dendrogram height \code{h}.}

\item{groups}{Optional grouping that overrides \code{k}/\code{h} for label colouring.
Either a \code{collapse_local()} result (its \verb{$mapping} is used) or a named
vector mapping each label to a group.}

\item{triangle}{If \code{TRUE} (default) draw only the upper triangle; otherwise
the full off-diagonal matrix. The diagonal is always dropped.}

\item{...}{Passed to the internal \code{.localtype_meta()} (e.g. \code{scale}, \code{ridge}).}
}
\value{
A \code{ggplot} object: the cross-region similarity heatmap.
}
\description{
Compares the GMM components of every region's local types in a common,
standardised predictor space and draws a meta-clustered distance heatmap.
Off-diagonal blocks that mix regions flag local types that are candidates for
collapsing with \code{\link[=collapse_local]{collapse_local()}}.
}
\seealso{
\code{\link[=collapse_local]{collapse_local()}}, \code{\link[=plot_local_embedding]{plot_local_embedding()}}
}
