% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{plot_local_embedding}
\alias{plot_local_embedding}
\title{Plot local types in a reduced environmental space}
\usage{
plot_local_embedding(
  local,
  data,
  memberships,
  collapse = NULL,
  dims = 2,
  n_max = 100,
  scale = TRUE,
  seed = NULL,
  samples = TRUE,
  ...
)
}
\arguments{
\item{local}{Fitted local-type object; supplies the predictor set via
\code{local$models}.}

\item{data}{All-sample predictor table, row-aligned to \code{memberships}.}

\item{memberships}{Wide local-type membership matrix; the argmax of each row
gives that sample's original type.}

\item{collapse}{Optional \code{collapse_local()} result; colours samples by merged
type and adds the collapsed centroids (diamonds).}

\item{dims}{2 (static ggplot2) or 3 (interactive plotly).}

\item{n_max}{Maximum number of samples drawn per original type in the scatter.}

\item{scale}{If \code{TRUE}, scale predictors to unit variance before the PCA.}

\item{seed}{Optional RNG seed for the per-type sample thinning.}

\item{samples}{If \code{TRUE}, draw the sample points; if \code{FALSE} they are made
fully transparent so only the centroids show (2D only).}

\item{...}{Currently unused; reserved for future arguments.}
}
\value{
For \code{dims = 2} a \code{ggplot} object; for \code{dims = 3} a \code{plotly} object.
}
\description{
PCA-embeds all samples, then overlays original-type centroids and, optionally,
the collapsed-type centroids from \code{\link[=collapse_local]{collapse_local()}}. Useful for checking
whether a proposed collapse merges types that genuinely overlap in predictor
space.
}
\seealso{
\code{\link[=collapse_local]{collapse_local()}}, \code{\link[=plot_local_similarity]{plot_local_similarity()}}
}
