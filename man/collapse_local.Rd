% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collpase_local_types.R
\name{collapse_local}
\alias{collapse_local}
\title{Collapse local types across regions into meta-types}
\usage{
collapse_local(
  local,
  memberships,
  k = NULL,
  h = NULL,
  similarity = NULL,
  distance = c("mean", "hellinger", "wasserstein"),
  sep = ".",
  ...
)
}
\arguments{
\item{local}{A fitted local-type object (as returned by \code{get_local_types()}).
Only used to (re)compute \code{similarity} when it is not supplied.}

\item{memberships}{The wide local-type membership matrix (catchments x types).
Column names must resolve to the internal meta labels (\verb{R<region>\\u00b7T<type>});
columns named \verb{Region<n>localType<g>} are relabelled to that form
automatically.}

\item{k, h}{Cut the meta-clustering into \code{k} meta-types or at dendrogram height
\code{h}. Supply exactly one of the two; supplying neither is an error.}

\item{similarity}{Optional precomputed \code{localtype_similarity} (from
\code{.localtype_meta()}); recomputed from \code{local} when \code{NULL}.}

\item{distance}{Distance used only when \code{similarity} is recomputed: \code{"mean"}
(component means), \code{"hellinger"} (mean and shape), or \code{"wasserstein"}.}

\item{sep}{Separator assumed between region and type in the membership column
labels; used when building the mismatch error message.}

\item{...}{Currently unused; reserved for future arguments.}
}
\value{
A list with components:
\describe{
\item{memberships}{Catchments x meta-types matrix of summed memberships.
Rows are \emph{not} renormalised, so they need not sum to 1.}
\item{hard}{Hard meta-type label per catchment (argmax of the normalised
memberships).}
\item{entropy}{Shannon entropy of each catchment's normalised membership
vector; higher values flag more ambiguous assignments.}
\item{row_sums}{Row sums of \code{memberships} before normalisation.}
\item{mapping}{Data frame linking each original type (\code{region}, \code{type},
\code{label}) to its meta-cluster (\code{meta}) and label (\code{meta_label}).}
\item{n_before,n_after}{Counts of local types before, and meta-types
after, collapsing.}
\item{k,h}{The cut arguments that were used.}
}
}
\description{
Cuts the cross-region meta-clustering (see \code{\link[=plot_local_similarity]{plot_local_similarity()}}) and
merges local types that fall in the same meta-cluster by summing their
membership columns. Meta-types are labelled sequentially (\code{meta.1}, \code{meta.2},
...) in increasing order of their meta-cluster id.
}
\details{
Run this after inspecting \code{\link[=plot_local_similarity]{plot_local_similarity()}} to commit to a particular
number of meta-types (\code{k}) or a dendrogram cut height (\code{h}).
}
\seealso{
\code{\link[=plot_local_similarity]{plot_local_similarity()}}, \code{\link[=plot_local_embedding]{plot_local_embedding()}}
}
