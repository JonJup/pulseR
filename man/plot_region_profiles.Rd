% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exploratory_graphics.R
\name{plot_region_profiles}
\alias{plot_region_profiles}
\title{Score Profiles Along Each Tuning Axis}
\usage{
plot_region_profiles(regions, score_col = "score", axes = NULL)
}
\arguments{
\item{regions}{A \code{"regions_tuning"} object (or its \code{tuning_log}).}

\item{score_col}{Column to plot. Default \code{"score"}.}

\item{axes}{Optional character vector restricting which axes are shown.}
}
\value{
A \code{ggplot} object.
}
\description{
One panel per axis that varies, showing the score along that axis with every
other axis pinned at the optimum. This is the general replacement for the
two-axis heat map: it works for any number of axes and, unlike a surface,
does not require the search to have been factorial, so it reads correctly for
\code{"sequential"} and \code{"iterative"} runs.
}
\details{
A flat profile means the axis does not matter over the range searched. A
profile still climbing at an endpoint means the candidate range was too
narrow, which is the usual finding for \code{n_st} and \code{partitions_per_tree}.
}
